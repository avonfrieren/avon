# Useless files
README.md

# Secrets — never commit real credentials
.env

# Rust build artifacts — regenerated by `cargo build`, huge, and
# platform-specific (no reason to version them)
/target

# Editor/OS cruft
.DS_Store
*.swp

# Claude Code local permission grants — machine-specific
.claude/settings.local.json
